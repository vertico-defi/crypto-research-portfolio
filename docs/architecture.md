# Architecture

Next.js components and the Node-built static compiler render versioned JSON snapshots. Source laboratories are read only. No checkout, order, payment, wallet, download, or server API implementation is present in the published repository.
