# Architecture

Next.js server components render static JSON snapshots. Source laboratories are read only. Checkout routes fail closed unless the configured live-store gate, private storage, legal consent, exact USDC verification, and idempotent order persistence are all available.
