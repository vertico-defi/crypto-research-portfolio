{
  "name": "crypto-research-portfolio",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "snapshot": "node scripts/generate-snapshot.mjs",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "next lint",
    "build": "next build",
    "check": "npm run snapshot && npm run test && npm run typecheck && npm run build"
  },
  "dependencies": {
    "next": "^16.2.12",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "@types/react": "18.3.12",
    "@types/react-dom": "18.3.1",
    "typescript": "5.7.2"
  }
}
