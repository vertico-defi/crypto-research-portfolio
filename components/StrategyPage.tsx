import { strategies } from "../lib/snapshot";
export function StrategyPage({id}:{id:string}){const s=strategies().find(x=>x.id===id);if(!s)return <p>Evidence record unavailable.</p>;return <><p className="status">{s.verdict} · CAPITAL PERMISSION 0</p><h1>{s.title}</h1><p className="lede">{s.disposition}</p><h2>Current evidence</h2><p>{s.warning}</p><h2>Methodology and limitations</h2><p>Source commit: <code>{s.sourceCommit}</code>. The snapshot is static, versioned, and does not query exchanges in the browser.</p><p>Latest source report: <code>{s.report}</code>.</p><h2>Engineering lesson</h2><p>Evidence gates, data contracts, and reproducible negative results are part of research quality.</p></>}
