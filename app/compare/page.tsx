import { strategies } from "../../lib/snapshot";
export default function Page(){return <><h1>Fair comparison</h1><p className="lede">Accepted-only is the default: no listed project has accepted trading P&amp;L, so the comparison is intentionally not a return leaderboard.</p><table><thead><tr><th>System</th><th>Track-record type</th><th>Comparable?</th><th>Integrity warning</th></tr></thead><tbody>{strategies().map(s=><tr key={s.id}><td>{s.title}</td><td>{s.pnl}</td><td>Not comparable</td><td>{s.warning}</td></tr>)}</tbody></table><h2>Benchmark contract</h2><p>BTC, ETH, and SOL buy-and-hold are normalized to 10,000 only for matched valid windows. They are not compared with gross, incomplete, prediction, or infrastructure outputs.</p></>}
