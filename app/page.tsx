import Link from "next/link"; import { products, strategies } from "../lib/snapshot";
export default function Home(){const list=strategies();return <><p className="status">STATIC RESEARCH SNAPSHOT · CAPITAL PERMISSION: 0</p><h1>Research systems, documented as they actually performed.</h1><p className="lede">Vertico Research presents reproducible engineering work, frozen protocols, negative findings, and infrastructure evidence without turning research outputs into trading claims.</p><div className="grid">{list.map(s=><article className="card" key={s.id}><p className="status">{s.verdict}</p><h2>{s.title}</h2><p>{s.warning}</p><Link href={`/strategies/${s.id}`}>Review evidence</Link></article>)}</div><h2>Research data products</h2><p>{products().filter(p=>p.status!=="PENDING_AUDIT_COMPLETION").length} drafts are documented. Checkout is disabled until rights, legal, and merchant gates are complete.</p><Link href="/data">View catalog</Link></>;}
